<UserControl x:Class="Otor.MsixHero.App.Modules.Packages.Views.PackagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:regions="http://prismlibrary.com/"
             xmlns:controls="clr-namespace:Otor.MsixHero.App.Controls"
             xmlns:constants="clr-namespace:Otor.MsixHero.App.Modules.Packages.Constants"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="200" />
            <ColumnDefinition Width="2*" MinWidth="150" />
        </Grid.ColumnDefinitions>
        <Border Margin="0 0 0 0">
            <Border Background="White" BorderBrush="#D2D2D2" BorderThickness="1 0">
                <ContentControl x:Name="Region" SizeChanged="RegionOnSizeChanged" regions:RegionManager.RegionName="{x:Static constants:PackagesRegionNames.Master}" />
            </Border>
        </Border>

        <ContentControl Grid.Column="1" regions:RegionManager.RegionName="{x:Static constants:PackagesRegionNames.Details}" />

        <GridSplitter Grid.Column="1" VerticalAlignment="Stretch" Width="10" Background="Transparent" HorizontalAlignment="Left" ResizeBehavior="PreviousAndCurrent" ResizeDirection="Columns" />
    </Grid>
</UserControl>
