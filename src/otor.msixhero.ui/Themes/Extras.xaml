<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:system="clr-namespace:System;assembly=System.Runtime"
                    xmlns:uac="clr-namespace:otor.msixhero.ui.Controls.Uac"
                    xmlns:helpers="clr-namespace:otor.msixhero.ui.Helpers">

    <Style TargetType="uac:UACControl">
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="uac:UACControl">
                    <Grid Margin="{TemplateBinding Padding}">
                        <ContentPresenter 
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}" />

                        <Image Visibility="{Binding ShowShield, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" 
                               VerticalAlignment="Top" 
                               HorizontalAlignment="Right"
                               Source="{x:Static helpers:ShieldIcon.Source}" 
                               Stretch="None" 
                               UseLayoutRounding="True" 
                               SnapsToDevicePixels="True">
                            <Image.RenderTransform>
                                <TranslateTransform X="8" />
                            </Image.RenderTransform>
                        </Image>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Path" x:Key="AnimatedSuccess">
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="StrokeDashCap" Value="Round" />
        <Setter Property="StrokeDashOffset" Value="20" />
        <Setter Property="Margin" Value="-40" />
        <Setter Property="Stroke" Value="DarkGreen" />
        <Setter Property="Width" Value="32" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Data" Value="M 10.050781 16.400391 L 14.699219 21.050781 L 22.349609 13.400391" />
        <Setter Property="LayoutTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="6.0" ScaleY="6.0" CenterX="16" CenterY="16" />
            </Setter.Value>
        </Setter>
        <Setter Property="StrokeDashArray">
            <Setter.Value>
                <DoubleCollection>
                    <system:Double>18</system:Double>
                    <system:Double>36</system:Double>
                </DoubleCollection>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsVisible" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard BeginTime="00:00:00.2">
                            <DoubleAnimationUsingKeyFrames AccelerationRatio="0.5" DecelerationRatio="0.5" BeginTime="00:00:00" Storyboard.TargetProperty="(Shape.StrokeDashOffset)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.75" Value="0"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>